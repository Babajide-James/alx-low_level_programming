#include "main.h"

/**
 * this function is used to check for lowercase character
 * I'm learning new things everytime
 * Return: Always 0
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{

	return (1);
	}
	return (0);
}
