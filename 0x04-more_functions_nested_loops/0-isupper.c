#include "main.h"

/**
 * this function  is used to check for uppercase character. Returns 1;
 * 0x: The number to be checked
 * Return: Always 0
 */

int _isupper(int c)

{

	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
