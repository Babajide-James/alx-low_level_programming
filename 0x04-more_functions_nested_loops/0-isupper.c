#include "main.h"

/**
 * isupper - this function checks for uppercase character
 * Return 1 if c is uppercase
 * Else, Alwys 0
 */

int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
