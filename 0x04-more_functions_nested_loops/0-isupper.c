#include "main.h"
#include <stdio.h>
/**
 * isupper - this function checks for uppercase character
 * Return: Always 0
 */

int _isupper(int x)

{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
