#include "main.h"


/**
 * mul - it multiplies two integers
 * The first integer number to multiply
 * The second to multiply the first number
 * Return: always 0
 */

int mul(inx x, int y)

{

	return (x * y);
}
